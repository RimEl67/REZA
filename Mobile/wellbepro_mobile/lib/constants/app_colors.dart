import 'package:flutter/material.dart';

class AppColors {
  // Primary brand — WellBePro green accent (matching Agenda page)
  static const Color primary = Color(0xFF10B981);
  static const Color primaryLight = Color(0xFF34D399);
  static const Color primaryDark = Color(0xFF059669);

  // Navy — used for header gradients and premium accents
  static const Color navy = Color(0xFF002366);
  static const Color navyLight = Color(0xFF1254C5);

  // Background — pure white like Agenda
  static const Color background = Color(0xFFFFFFFF);
  static const Color surfaceGray = Color(0xFFFAFAFA);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Text
  static const Color textDark = Color(0xFF111827);
  static const Color textGray = Color(0xFF6B7280);
  static const Color textLight = Color(0xFF9CA3AF);

  // Status colors
  static const Color confirmed = Color(0xFF10B981);
  static const Color confirmedBg = Color(0xFFD1FAE5);
  static const Color pending = Color(0xFFF59E0B);
  static const Color pendingBg = Color(0xFFFEF3C7);
  static const Color cancelled = Color(0xFFEF4444);
  static const Color cancelledBg = Color(0xFFFEE2E2);

  // Border
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFF3F4F6);

  // Nav
  static const Color navActive = Color(0xFF10B981);
  static const Color navInactive = Color(0xFF9CA3AF);

  // Accent
  static const Color accent = Color(0xFF10B981);
}
